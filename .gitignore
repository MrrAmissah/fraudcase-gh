node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
!.env.staging.example

# Local evidence file copies written by the upload handler (contains user PII)
secure_uploads/

# Service-account / admin credential keys (never commit real secrets)
*.serviceAccount.json
service-account*.json
firebase-adminsdk*.json

# Local-only AI/operator handoff + scratch notes (never committed)
.ai-local/
